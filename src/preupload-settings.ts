const add=()=>{const card=Array.from(document.querySelectorAll('.tool-card')).find(x=>x.textContent?.includes('画像結合'));if(!card||card.querySelector('.preupload-settings'))return;const drop=card.querySelector('.drop');if(!drop)return;const box=document.createElement('div');box.className='preupload-settings';box.innerHTML='<b>結合設定</b><span>画像を追加すると、この設定で結合できます</span><div><button>縦方向</button><button>横方向</button><label>間隔 <input type="range" min="0" max="100" value="0"></label></div>';drop.before(box)};new MutationObserver(add).observe(document.body,{childList:true,subtree:true});setInterval(add,300);
